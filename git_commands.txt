
 ( the git commands I have used )  

  * git init
  * git status
  * git add .
  * git commit
  * git log
  * git commit -m " "
  * git commit -am " "
  * git branch
  * git branch -M main
  * git remote
  * git remote add 
  * git remote -v
  * git push origin main
  * git pull origin main
  * git pull origin --allow-unrelated-histories

  
